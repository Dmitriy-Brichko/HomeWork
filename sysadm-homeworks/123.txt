#!/usr/bin/env python3

import os

bash_command = ["cd /c/Users/admin/netology/sysadm-homeworks", "git status"] #изменил путь на понятный
result_os = os.popen(' && '.join(bash_command)).read()
#is_change = False  ***не нужная строка***
for result in result_os.split('\n'):
    if result.find('modified') != -1:
        prepare_result = result.replace('\tmodified:   ', '')
        print(prepare_result)
#        break  ***строка не нужна, т.к. завершается скрипт на первом найденом измененном файле***